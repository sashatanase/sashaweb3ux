# Analytics log

Output of the [Analytics Agent](../../.github/analytics-agent/README.md).

- `data/<YYYY-MM>.jsonl` — one daily KPI snapshot per line (appended each day).
- `reports/<YYYY>-W<WW>.md` — the weekly review written by the agent, with
  recommendations and a link to that week's improvement PR (if any).
