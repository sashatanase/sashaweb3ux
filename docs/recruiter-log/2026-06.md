# 2026-06

## 2026-06-22 — IMPROVE

The case-study writing and domain credibility are strong (real clients —
Threshold Network, Mezo, Keep Network — specific methods, real metrics like
the 73.125/100 SUS score on the tBTC diary study, and concrete roadmap
impact). That alone would normally earn a CONTACT.

What's blocking it: the live capture showed the résumé link and all six
"Read the full report" case-study PDFs 404ing, plus the home-page portrait
photo failing to load (raw alt text on mobile). Traced the cause to an
external Lovable asset-CDN path (`/__l5e/assets-v1/...`) with no proxy or
binding anywhere in this repo — not something fixable from source without
the original binaries. Shipped a graceful image-load fallback for the
portrait and opened a PR flagging the asset-pipeline issue for a human to
verify against the real deployed domain.

PR: https://github.com/sashatanase/sashaweb3ux/pull/21

## 2026-06-22 — CONTACT

Re-reviewed after the asset-CDN fix landed (PR #21, merged). All six
case-study PDFs and the résumé now return 200 locally, and the home-page
portrait renders on both desktop and mobile — the blocker from earlier
today is gone. The underlying work was always strong: the tBTC diary
study turns a 73.125/100 SUS score into three specific, product-changing
findings (hardware-downgrade slashing risk, a false-success banner masking
broken nodes, address-field overload defeating the security model), and
the Mezo/Keep Network studies show the same pattern with named methods
(JTBD, PMF segmentation) and real client names. The remaining "broken"
links (LinkedIn 999, Medium 403) are bot-blocking responses, not site
defects — verified with a browser UA. Site craft, mobile layout, and
contact path are all clean. Decision flips to CONTACT.

Issue: https://github.com/sashatanase/sashaweb3ux/issues/24

## 2026-06-26 — CONTACT (verdict holds)

Re-reviewed the live capture; no source changes have landed since the
2026-06-22 fix. All six case-study PDFs, the résumé, and the home-page
portrait still load cleanly (200s) on both desktop and mobile, and the
tBTC/Mezo/Keep Network case studies still carry the same product-changing
findings and named methods that earned the CONTACT call. The "failed
requests" in today's capture are Google Analytics beacons aborted by the
headless capture environment (not a site defect), and the only "broken"
links are the same bot-blocked Medium (403) and LinkedIn (405) responses
seen previously. No new signal — leaving the existing outreach issue as-is
rather than adding a duplicate comment.

Issue: https://github.com/sashatanase/sashaweb3ux/issues/24

## 2026-06-27 — CONTACT (verdict holds)

Re-reviewed today's live capture; still no source changes since the
2026-06-22 fix, confirmed via git log. Home page (desktop + mobile)
renders cleanly — portrait loads, hero, selected work, case studies,
recent writing, and the "Let's talk" contact block with résumé/LinkedIn/
GitHub/Twitter links all present and legible. The only flagged issues are
the same Google Analytics beacon aborts (capture-environment artifact)
and the same bot-blocked Medium (403) and LinkedIn (405) link-checker
responses seen on 06-22 and 06-26 — not site defects. No new signal, so
the existing outreach issue stands untouched.

Issue: https://github.com/sashatanase/sashaweb3ux/issues/24
